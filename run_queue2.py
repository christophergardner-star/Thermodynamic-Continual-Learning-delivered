from __future__ import annotations


def main() -> int:
    print(
        "run_queue2.py has been retired and moved to legacy_quarantine/run_queue2.py.\n"
        "Queue promotion into autonomous execution is no longer supported.\n"
        "Review proposals in the dashboard and use committed manifests for bounded runs.",
        flush=True,
    )
    return 2


if __name__ == "__main__":
    raise SystemExit(main())
