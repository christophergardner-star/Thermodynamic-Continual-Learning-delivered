from __future__ import annotations


def main() -> int:
    print(
        "tar_autonomous_research.py has been retired and moved to legacy_quarantine/tar_autonomous_research.py.\n"
        "Open-ended autonomous research execution is no longer supported.\n"
        "TAR may propose work, but execution now requires human approval and a committed manifest.",
        flush=True,
    )
    return 2


if __name__ == "__main__":
    raise SystemExit(main())
